---
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import { SITE_DESCRIPTION_ES, PURCHASE_LINKS, ROUTES } from '../../consts';
import coverES from '../../assets/cover-es.jpg';
import jorgePhoto from '../../assets/jorge-profile.jpg';
import carlosPhoto from '../../assets/carlos-profile.jpg';

const lang = 'es';
---

<Layout description={SITE_DESCRIPTION_ES} lang={lang}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="display">IA de Piloto a Escala</h1>
        <p class="hero-subtitle">Transforma tu empresa con inteligencia artificial</p>
        <div class="hero-ctas">
          <a href={PURCHASE_LINKS.es} class="btn btn-primary" target="_blank" rel="noopener">
            Comprar Ahora
          </a>
          <a href={ROUTES.book.es} class="btn btn-secondary">
            Saber Más
          </a>
        </div>
      </div>
      <div class="hero-image">
        <Image
          src={coverES}
          alt="La IA en los Negocios - Portada del libro en español"
          class="book-cover"
          width={600}
          densities={[1, 2]}
        />
      </div>
    </div>
  </section>

  <!-- Value Section -->
  <section class="value-section">
    <div class="container">
      <h2 class="section-heading">Por qué este libro</h2>
      <div class="value-grid">
        <div class="value-card">
          <div class="value-icon">60+</div>
          <h3>Casos reales</h3>
          <p>De Adidas a Zilliant, descubre cómo empresas líderes logran crecer hasta un 650% con IA.</p>
        </div>
        <div class="value-card">
          <div class="value-icon">5</div>
          <h3>Frameworks probados</h3>
          <p>Herramientas propias como MELDS+ y el diagnóstico AIM-DDI, desarrolladas desde la experiencia.</p>
        </div>
        <div class="value-card">
          <div class="value-icon">1.400+</div>
          <h3>Validado por ejecutivos</h3>
          <p>Construido a partir de la formación de directivos en ESADE y asesoramiento a empresas en múltiples sectores.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Authors Preview -->
  <section class="authors-preview">
    <div class="container">
      <h2 class="section-heading">Conoce a los Autores</h2>
      <div class="authors-grid">
        <div class="author-preview-card">
            <Image
              src={jorgePhoto}
              alt="Jorge Calvo"
              class="author-photo"
              width={360}
              densities={[1, 2]}
            />
            <h3>Jorge Calvo</h3>
            <p class="author-credential">Vicedecano de GLOBIS, Asesor de Estrategia de Negocios</p>
          </div>
          <div class="author-preview-card">
            <Image
              src={carlosPhoto}
              alt="Carlos Escapa"
              class="author-photo"
              width={360}
              densities={[1, 2]}
            />
            <h3>Carlos Escapa</h3>
            <p class="author-credential">Autor, ex-Meta, AWS Data & AI Director de Desarrollo de Negocios</p>
          </div>
      </div>
      <div class="authors-cta">
        <a href={ROUTES.authors.es} class="btn btn-ghost">
          Leer Biografías Completas &rarr;
        </a>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="final-cta">
    <div class="container text-center">
      <h2>Lidera la transformación con IA</h2>
      <p>El futuro pertenece a quienes combinan imaginación humana con inteligencia artificial. Empieza hoy.</p>
      <a href={PURCHASE_LINKS.es} class="btn btn-primary btn-large" target="_blank" rel="noopener">
        Obtén tu Copia
      </a>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: 96px 24px;
    background: var(--bg-primary);
  }

  .hero-content {
    max-width: 1280px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  .hero-text {
    max-width: 560px;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin: 24px 0 32px;
  }

  .hero-ctas {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero-image {
    display: flex;
    justify-content: center;
  }

  .book-cover {
    width: 300px;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  /* Value Section */
  .value-section {
    padding: 96px 24px;
    background: var(--bg-secondary);
  }

  .section-heading {
    text-align: center;
    margin-bottom: 48px;
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .value-card {
    background: var(--bg-primary);
    padding: 32px;
    border-radius: 12px;
    text-align: center;
  }

  .value-icon {
    width: 64px;
    height: 64px;
    background: var(--accent-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 24px;
    color: var(--text-inverse);
    font-size: 0.875rem;
  }

  .value-card h3 {
    margin-bottom: 12px;
  }

  .value-card p {
    color: var(--text-secondary);
    margin: 0;
  }

  /* Authors Preview */
  .authors-preview {
    padding: 96px 24px;
  }

  .authors-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 48px;
    max-width: 800px;
    margin: 0 auto;
  }

  .author-preview-card {
    text-align: center;
  }

  .author-photo {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto 24px;
  }

  .author-preview-card h3 {
    margin-bottom: 8px;
  }

  .author-credential {
    color: var(--text-secondary);
    margin: 0;
  }

  .authors-cta {
    text-align: center;
    margin-top: 48px;
  }

  /* Final CTA */
  .final-cta {
    padding: 96px 24px;
    background: var(--bg-secondary);
  }

  .final-cta h2 {
    margin-bottom: 16px;
  }

  .final-cta p {
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 32px;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: 16px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s;
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn-primary {
    background: var(--accent-primary);
    color: var(--text-inverse);
  }

  .btn-primary:hover {
    opacity: 0.9;
  }

  .btn-secondary {
    background: transparent;
    border: 2px solid var(--accent-primary);
    color: var(--accent-primary);
  }

  .btn-secondary:hover {
    background: var(--accent-primary);
    color: var(--text-inverse);
  }

  .btn-ghost {
    background: transparent;
    color: var(--accent-primary);
    padding: 8px 0;
  }

  .btn-ghost:hover {
    text-decoration: underline;
  }

  .btn-large {
    padding: 20px 48px;
    font-size: 1.125rem;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-text {
      max-width: 100%;
    }

    .hero-ctas {
      justify-content: center;
    }

    .value-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 64px 24px;
    }

    .value-section,
    .authors-preview,
    .final-cta {
      padding: 64px 24px;
    }

    .hero-content {
      gap: 48px;
    }

    .book-cover {
      width: 240px;
    }

    .authors-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .author-photo {
      width: 140px;
      height: 140px;
    }
  }
</style>
