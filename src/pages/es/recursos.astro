---
import Layout from '../../layouts/Layout.astro';
import { ROUTES } from '../../consts';

const lang = 'es';
const pageTitle = 'Recursos';
const pageDescription = 'Recursos gratuitos, contenido descargable y capítulos de muestra de La IA en los Negocios.';

const resources = [
  {
    title: 'Capítulo de Muestra',
    description: 'Descarga un capítulo de muestra gratuito para conocer el estilo y contenido del libro. Obtén un adelanto de conocimientos prácticos sobre IA.',
    type: 'PDF',
    downloadURL: '#',
  },
  {
    title: 'Glosario de IA',
    description: 'Un glosario completo de términos de IA que todo líder empresarial debe conocer. Mantén esta referencia a mano.',
    type: 'PDF',
    downloadURL: '#',
  },
  {
    title: 'Guía de Casos de Uso',
    description: 'Ejemplos reales de implementación de IA en diversas industrias. Ve cómo las empresas líderes están usando la IA hoy.',
    type: 'PDF',
    downloadURL: '#',
  },
];
---

<Layout title={pageTitle} description={pageDescription} lang={lang}>
  <!-- Breadcrumb -->
  <nav class="breadcrumb" aria-label="Breadcrumb">
    <div class="container">
      <a href={ROUTES.home.es}>Inicio</a>
      <span class="separator">/</span>
      <span aria-current="page">Recursos</span>
    </div>
  </nav>

  <!-- Page Header -->
  <section class="page-header">
    <div class="container">
      <h1>Recursos</h1>
      <p class="page-intro">Los siguientes documentos se pueden descargar gratuitamente.</p>
    </div>
  </section>

  <!-- Resources Grid -->
  <section class="resources-section">
    <div class="container">
      <div class="resources-grid">
        {resources.map((resource) => (
          <article class="resource-card">
            <h2>{resource.title}</h2>
            <p>{resource.description}</p>
            <a href={resource.downloadURL} class="btn btn-primary" download>
              <span>Descargar</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                <path d="M8 12L3 7l1.4-1.4L7 8.2V1h2v7.2l2.6-2.6L13 7l-5 5z" fill="currentColor"/>
                <path d="M13 14H3v-2H1v4h14v-4h-2v2z" fill="currentColor"/>
              </svg>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="resources-cta">
    <div class="container text-center">
      <h2>¿Quieres la Guía Completa?</h2>
      <p>Estos recursos son solo una muestra. Obtén el libro completo para estrategias integrales de IA en los negocios.</p>
      <a href={ROUTES.book.es} class="btn btn-primary">
        Conocer Más Sobre el Libro
      </a>
    </div>
  </section>
</Layout>

<style>
  /* Breadcrumb */
  .breadcrumb {
    padding: 16px 0;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .breadcrumb a {
    color: var(--text-secondary);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    color: var(--accent-primary);
  }

  .breadcrumb .separator {
    margin: 0 8px;
  }

  /* Page Header */
  .page-header {
    padding: 48px 0 64px;
    text-align: center;
  }

  .page-header h1 {
    margin-bottom: 24px;
  }

  .page-intro {
    max-width: 680px;
    margin: 0 auto;
    color: var(--text-secondary);
    font-size: 1.125rem;
  }

  /* Resources Section */
  .resources-section {
    padding: 0 0 96px;
  }

  .resources-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .resource-card {
    background: var(--bg-secondary);
    padding: 32px;
    border-radius: 12px;
    display: flex;
    flex-direction: column;
  }

  .resource-card h2 {
    font-size: 1.5rem;
    margin-bottom: 12px;
  }

  .resource-card p {
    color: var(--text-secondary);
    margin-bottom: 24px;
    flex-grow: 1;
  }

  .resource-card .btn {
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    box-shadow: 0 2px 8px rgba(58, 175, 175, 0.3);
  }

  .resource-card .btn:hover {
    box-shadow: 0 4px 12px rgba(58, 175, 175, 0.4);
  }

  .resource-card .btn svg {
    flex-shrink: 0;
  }

  /* CTA */
  .resources-cta {
    padding: 96px 24px;
    background: var(--bg-secondary);
  }

  .resources-cta h2 {
    margin-bottom: 16px;
  }

  .resources-cta p {
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 32px;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: 16px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s;
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn-primary {
    background: var(--accent-primary);
    color: var(--text-inverse);
  }

  .btn-primary:hover {
    opacity: 0.9;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .resources-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .resources-section {
      padding-bottom: 64px;
    }

    .resources-grid {
      grid-template-columns: 1fr;
    }

    .resources-cta {
      padding: 64px 24px;
    }
  }
</style>
