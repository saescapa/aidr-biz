---
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import { SITE_TITLE_EN, SITE_DESCRIPTION_EN, PURCHASE_LINKS, AUTHORS, ROUTES } from '../../consts';
import coverEN from '../../assets/cover-en.jpg';

const lang = 'en';
---

<Layout title={SITE_TITLE_EN} description={SITE_DESCRIPTION_EN} lang={lang}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="display">AI from Pilot to Scale</h1>
        <p class="hero-subtitle">A practical guide to transforming your business with artificial intelligence</p>
        <div class="hero-ctas">
          <a href={PURCHASE_LINKS.en} class="btn btn-primary" target="_blank" rel="noopener">
            Buy Now
          </a>
          <a href={ROUTES.book.en} class="btn btn-secondary">
            Learn More
          </a>
        </div>
      </div>
      <div class="hero-image">
        <Image
          src={coverEN}
          alt="AI in Business - English Edition book cover"
          class="book-cover"
          width={300}
        />
      </div>
    </div>
  </section>

  <!-- Value Section -->
  <section class="value-section">
    <div class="container">
      <h2 class="section-heading">Why This Book</h2>
      <div class="value-grid">
        <div class="value-card">
          <div class="value-icon">60+</div>
          <h3>Real-World Case Studies</h3>
          <p>From Amazon to MYbank, see how leading companies achieve 650% performance gains with strategic AI integration.</p>
        </div>
        <div class="value-card">
          <div class="value-icon">5</div>
          <h3>Proven Frameworks</h3>
          <p>Proprietary tools including the AI Moat, MELDS+, and AIM-DDI diagnostic developed from hands-on transformation experience.</p>
        </div>
        <div class="value-card">
          <div class="value-icon">1,400+</div>
          <h3>Executive-Tested</h3>
          <p>Built from training executives at ESADE and advising companies across multiple sectors globally.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Authors Preview -->
  <section class="authors-preview">
    <div class="container">
      <h2 class="section-heading">Meet the Authors</h2>
      <div class="authors-grid">
        {AUTHORS.map((author) => (
          <div class="author-preview-card">
            <div class="author-photo-placeholder">
              <span>Photo</span>
            </div>
            <h3>{author}</h3>
            <p class="author-credential">Lead, AI in Business Program at ESADE</p>
          </div>
        ))}
      </div>
      <div class="authors-cta">
        <a href={ROUTES.authors.en} class="btn btn-ghost">
          Read Full Bios &rarr;
        </a>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="final-cta">
    <div class="container text-center">
      <h2>Lead the AI-Driven Transformation</h2>
      <p>The future belongs to those who combine human imagination with machine intelligence. Start today.</p>
      <a href={PURCHASE_LINKS.en} class="btn btn-primary btn-large" target="_blank" rel="noopener">
        Get Your Copy
      </a>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: 96px 24px;
    background: var(--bg-primary);
  }

  .hero-content {
    max-width: 1280px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  .hero-text {
    max-width: 560px;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin: 24px 0 32px;
  }

  .hero-ctas {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero-image {
    display: flex;
    justify-content: center;
  }

  .book-cover {
    width: 300px;
    height: auto;
    border-radius: 12px;
    border: 1px solid color-mix(in srgb, white 20%, transparent);
    box-shadow:
      0 25px 50px color-mix(in srgb, var(--text-primary) 25%, transparent),
      0 10px 20px color-mix(in srgb, var(--text-primary) 15%, transparent),
      inset 0 1px 0 color-mix(in srgb, white 20%, transparent);
  }

  /* Value Section */
  .value-section {
    padding: 96px 24px;
    background: var(--bg-secondary);
  }

  .section-heading {
    text-align: center;
    margin-bottom: 48px;
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .value-card {
    /* Liquid Glass card */
    position: relative;
    padding: 32px;
    border-radius: 16px;
    text-align: center;
    background: color-mix(in srgb, var(--bg-primary) 60%, transparent);
    backdrop-filter: blur(40px) saturate(200%) brightness(1.05);
    -webkit-backdrop-filter: blur(40px) saturate(200%) brightness(1.05);
    border: 1px solid color-mix(in srgb, white 30%, transparent);
    box-shadow:
      0 4px 24px color-mix(in srgb, var(--text-primary) 10%, transparent),
      inset 0 1px 1px color-mix(in srgb, white 50%, transparent);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .value-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(
      180deg,
      color-mix(in srgb, white 25%, transparent) 0%,
      color-mix(in srgb, white 10%, transparent) 40%,
      transparent 100%
    );
    pointer-events: none;
    border-radius: 16px 16px 0 0;
  }

  .value-card:hover {
    transform: translateY(-4px);
    box-shadow:
      0 8px 32px color-mix(in srgb, var(--text-primary) 15%, transparent),
      inset 0 1px 1px color-mix(in srgb, white 60%, transparent);
  }

  .value-icon {
    position: relative;
    z-index: 1;
    width: 64px;
    height: 64px;
    background: color-mix(in srgb, var(--accent-primary) 85%, transparent);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 24px;
    color: var(--text-inverse);
    font-size: 0.875rem;
    font-weight: 700;
    border: 1px solid color-mix(in srgb, white 30%, transparent);
    box-shadow:
      0 4px 16px color-mix(in srgb, var(--accent-primary) 40%, transparent),
      inset 0 1px 2px color-mix(in srgb, white 40%, transparent);
  }

  .value-card h3 {
    position: relative;
    z-index: 1;
    margin-bottom: 12px;
  }

  .value-card p {
    position: relative;
    z-index: 1;
    color: var(--text-secondary);
    margin: 0;
  }

  /* Authors Preview */
  .authors-preview {
    padding: 96px 24px;
  }

  .authors-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 48px;
    max-width: 800px;
    margin: 0 auto;
  }

  .author-preview-card {
    text-align: center;
  }

  .author-photo-placeholder {
    position: relative;
    width: 180px;
    height: 180px;
    background: color-mix(in srgb, var(--bg-secondary) 60%, transparent);
    backdrop-filter: blur(30px) saturate(180%);
    -webkit-backdrop-filter: blur(30px) saturate(180%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 24px;
    color: var(--text-secondary);
    font-weight: 500;
    border: 1px solid color-mix(in srgb, white 25%, transparent);
    box-shadow:
      0 8px 32px color-mix(in srgb, var(--text-primary) 12%, transparent),
      inset 0 2px 4px color-mix(in srgb, white 40%, transparent);
    overflow: hidden;
  }

  .author-photo-placeholder::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 45%;
    background: linear-gradient(
      180deg,
      color-mix(in srgb, white 30%, transparent) 0%,
      transparent 100%
    );
    pointer-events: none;
  }

  .author-preview-card h3 {
    margin-bottom: 8px;
  }

  .author-credential {
    color: var(--text-secondary);
    margin: 0;
  }

  .authors-cta {
    text-align: center;
    margin-top: 48px;
  }

  /* Final CTA */
  .final-cta {
    padding: 96px 24px;
    background: var(--bg-secondary);
  }

  .final-cta h2 {
    margin-bottom: 16px;
  }

  .final-cta p {
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 32px;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: 16px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s;
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn-primary {
    position: relative;
    background: color-mix(in srgb, var(--accent-primary) 85%, transparent);
    backdrop-filter: blur(12px) saturate(180%);
    -webkit-backdrop-filter: blur(12px) saturate(180%);
    color: var(--text-inverse);
    border: 1px solid color-mix(in srgb, white 25%, transparent);
    box-shadow:
      0 4px 16px color-mix(in srgb, var(--accent-primary) 35%, transparent),
      inset 0 1px 2px color-mix(in srgb, white 35%, transparent);
    overflow: hidden;
  }

  .btn-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(
      180deg,
      color-mix(in srgb, white 25%, transparent) 0%,
      transparent 100%
    );
    pointer-events: none;
  }

  .btn-primary:hover {
    background: color-mix(in srgb, var(--accent-primary) 95%, transparent);
    box-shadow:
      0 6px 24px color-mix(in srgb, var(--accent-primary) 45%, transparent),
      inset 0 1px 2px color-mix(in srgb, white 40%, transparent);
  }

  .btn-secondary {
    position: relative;
    background: color-mix(in srgb, var(--bg-primary) 50%, transparent);
    backdrop-filter: blur(12px) saturate(150%);
    -webkit-backdrop-filter: blur(12px) saturate(150%);
    border: 1px solid color-mix(in srgb, var(--accent-primary) 60%, transparent);
    color: var(--accent-primary);
    box-shadow:
      inset 0 1px 2px color-mix(in srgb, white 20%, transparent);
    overflow: hidden;
  }

  .btn-secondary::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(
      180deg,
      color-mix(in srgb, white 15%, transparent) 0%,
      transparent 100%
    );
    pointer-events: none;
  }

  .btn-secondary:hover {
    background: color-mix(in srgb, var(--accent-primary) 85%, transparent);
    color: var(--text-inverse);
    border-color: color-mix(in srgb, white 25%, transparent);
    box-shadow:
      0 4px 16px color-mix(in srgb, var(--accent-primary) 35%, transparent),
      inset 0 1px 2px color-mix(in srgb, white 35%, transparent);
  }

  .btn-ghost {
    background: transparent;
    color: var(--accent-primary);
    padding: 8px 0;
  }

  .btn-ghost:hover {
    text-decoration: underline;
  }

  .btn-large {
    padding: 20px 48px;
    font-size: 1.125rem;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-text {
      max-width: 100%;
    }

    .hero-ctas {
      justify-content: center;
    }

    .value-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 64px 24px;
    }

    .value-section,
    .authors-preview,
    .final-cta {
      padding: 64px 24px;
    }

    .hero-content {
      gap: 48px;
    }

    .book-cover {
      width: 240px;
    }

    .authors-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .author-photo-placeholder {
      width: 140px;
      height: 140px;
    }
  }
</style>
