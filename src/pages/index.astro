---
// Root index redirects to language-specific homepage
// Default to English, could detect browser language in the future

const BASE = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${BASE}/en/`);
---
